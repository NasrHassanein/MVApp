---
version: '3.7'
services:
  shiny:
    build:
      context: .
    image: mvapp:latest
    container_name: mvapp
    restart: always
    volumes:
      - /var/log:/var/log
    ports:
      - "80:80"
